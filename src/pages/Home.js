import Weather from "../components/Weather"
const Home = (props) => {
    return (<div className="box background-tint">
        <h1> Homepage</h1>
        <Weather />

    </div>
    )
}

export default Home